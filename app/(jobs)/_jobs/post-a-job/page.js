// import sub components
import JobsList from './Index';

export const metadata = {
  title: "Post A Job | Geeks Nextjs Template",
}

const Page = () => {
  return (<JobsList />);
};

export default Page;
