// import sub components
import JobsList from './Index';

export const metadata = {
    title: "Job Listing in List and Grid View | Geeks Nextjs Template",
}

const Page = () => {
    return (<JobsList />);
};

export default Page;
