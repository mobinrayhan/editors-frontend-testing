// import sub components
import Jobs from './Index';

export const metadata = {
  title: "Jobs at Company | Jobs | Geeks Nextjs Template",
}

const Page = () => {
  return (<Jobs />);
};

export default Page;
